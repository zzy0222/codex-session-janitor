{
  "name": "codex-session-janitor",
  "version": "0.1.0",
  "description": "A small Codex session retention module with CLI and TUI cleanup workflows.",
  "type": "module",
  "bin": {
    "codex-session-janitor": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/cli.ts",
    "test": "vitest run",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "keywords": [
    "codex",
    "session",
    "cleanup",
    "retention",
    "tui"
  ],
  "license": "MIT",
  "dependencies": {
    "commander": "^14.0.2",
    "ink": "^6.5.1",
    "pretty-bytes": "^7.1.0",
    "react": "^19.1.1",
    "trash": "^10.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.10.2",
    "@types/react": "^19.2.7",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.16"
  },
  "engines": {
    "node": ">=20"
  }
}
